# Project Instructions

- When packaging the extension, always place the VSIX output in the `release/` folder.
- Use the version from `package.json` for the VSIX filename: `release/market-monitoring-<version>.vsix`.
- Prefer `npm run package` for packaging so the release output path is applied consistently.
